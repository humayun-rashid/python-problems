"""
Write a function fraction_avg(f_list), that gets a list of fractions as an argument, and
proceeds to calculate and return the average of all fractions in that list. Use class Fraction from
module fractions.
Example run (in Python Shell):
m = [Fraction(1,2), Fraction(1,4), Fraction(1,3)]
print (fraction_avg(m))
13/36
"""
def fraction_avg(f_list):
    